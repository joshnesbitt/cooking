<!DOCTYPE html>
<html lang="en">
  <head>
    {% include meta %}

    <meta name="description" content="{{ title }}" />

    <title>{{ title }}</title>
  </head>
  <body>
    <div class="container post-container">
        <h1>{{ title }}</h1>
      <div class='content'>{{ content }}</div>
      <div class="back-link">
        <a href="/">← Back</a>
      </div>
    </div>
  </body>
</html>
