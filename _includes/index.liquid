<!DOCTYPE html>
<html lang="en">
  <head>
    {% include meta %}
  </head>
  <body>
    <div class="o-container">
      <div class="o-markdown">
        <h1>{{ title }}</h1>

        {{ content }}
      </div>

      <ul class="o-posts u-margin-top">
        {% for post in collections.posts reversed %}
          <li class="o-posts__item">
            <a href="{{ post.url }}">
              {% if post.data.type == 'drink' %}
              🍸
              {% else %}
              🍴
              {% endif %}

              {{ post.data.title }}
            </a>

            <span class="o-posts__date">
              {{ post.date | date: "%b %Y" }}
            </span>
          </li>
        {% endfor %}
      </ul>
    </div>

    {% include footer %}
  </body>
</html>
