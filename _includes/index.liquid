<!DOCTYPE html>
<html lang="en">
  <head>
    {% include meta %}

    <meta name="description" content="Josh Nesbitt Cooking" />

    <title>{{ title }}</title>
  </head>
  <body>
    <div class="container">
      {{ content }}

      <!-- <h4 class="category">Posts</h4> -->

      <ul class="posts">
        {% for post in collections.posts reversed %}
          <li class="post">
            <span class="post-title">
              <a href="{{ post.url }}">{{ post.data.title }}</a>
            </span>

            <span class="post-date">
              {{ post.date | date: "%b %Y" }}
            </span>
          </li>
        {% endfor %}
      </ul>
    </div>
  </body>
</html>
