<!DOCTYPE html>
<html lang="en">
  <head>
    {% include meta %}
    <meta name="description" content="Cooking Diary" />
    <title>{{ title }}</title>
  </head>
  <body>
    <div class="o-container">
      <div class="o-markdown">
        {{ content }}
      </div>

      <ul class="o-posts u-margin-top">
        {% for post in collections.posts reversed %}
          <li class="o-posts__item">
            <a href="{{ post.url }}">{{ post.data.title }}</a>

            <span class="o-posts__date">
              {{ post.date | date: "%b %Y" }}
            </span>
          </li>
        {% endfor %}
      </ul>
    </div>
  </body>
</html>
